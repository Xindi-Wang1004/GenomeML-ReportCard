# Tutorial 3: Render a report from a public-style manifest (~5 min)

```bash
ls tests/toy_data/manifest_with_block.tsv
ls tests/toy_data/report_pass.json
# Inspect schema:
ls schemas/reportcard_report.schema.json
```

Compare rendered Markdown under `tests/toy_data/report_pass.md`.
